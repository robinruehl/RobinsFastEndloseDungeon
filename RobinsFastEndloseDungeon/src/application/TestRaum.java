package application;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;
import org.junit.Test;

public class TestRaum {
	@Test
	public void test() {
		/*
		Raum r1 = new Raum();
		assertEquals("placeholder", r1.getBeschreibung());
		assertTrue("nord ist nicht gleich null.", r1.getNordRaum() == null);
		Raum r2 = new Raum();
		r2.setNordRaum(r1);
		assertTrue("Der Nord Raum wurde nicht zugeteilt", r2.getNordRaum() != null);
		r2.setSuedRaum(r1);
		assertTrue("Der Sued Raum wurde nicht zugeteilt", r2.getSuedRaum() != null);
		r2.setOstRaum(r1);
		assertTrue("Der Ost Raum wurde nicht zugeteilt", r2.getOstRaum() != null);
		r2.setWestRaum(r1);
		assertTrue("Der West Raum wurde nicht zugeteilt", r2.getWestRaum() != null);
		Raum r3 = new Raum("Test");
		assertTrue("Der Raum hat nicht die Beschreibung Test", r3.getBeschreibung() == "Test");
		Raum r4 = new Raum("Test", true);
		assertTrue("Der Raum hat nicht die Beschreibung Test", r4.getBeschreibung() == "Test");
		assertTrue("Der Raum hat keinen Gegner", r4.isEnemy());
		Raum r5 = new Raum("Test", true, true);
		assertTrue("Der Raum hat nicht die Beschreibung Test", r5.getBeschreibung() == "Test");
		assertTrue("Der Raum hat keinen Gegner", r5.isEnemy());
		assertTrue("Der Raum hat keinen Gegner", r5.isBoss());
		Raum r6 = new Raum("Test", r1, r2, r3, r4, true);
		assertTrue("Der Raum hat nicht die Beschreibung Test", r6.getBeschreibung() == "Test");
		assertTrue("Der Raum hat keinen Gegner", r6.isEnemy());
		assertTrue("Der Nord Raum wurde nicht richtig zugeteilt", r6.getNordRaum() == r1);
		assertTrue("Der Sued Raum wurde nicht richtig zugeteilt", r6.getSuedRaum() == r3);
		assertTrue("Der Ost Raum wurde nicht richtig zugeteilt", r6.getOstRaum() == r2);
		assertTrue("Der West Raum wurde nicht richtig zugeteilt", r6.getWestRaum() == r4);
		Raum r7 = new Raum("Test", r1, r2, r3, r4, true, true);
		assertTrue("Der Raum hat nicht die Beschreibung Test", r7.getBeschreibung() == "Test");
		assertTrue("Der Raum hat keinen Gegner", r7.isEnemy());
		assertTrue("Der Raum hat keinen Boss", r7.isBoss());
		assertTrue("Der Nord Raum wurde nicht richtig zugeteilt", r7.getNordRaum() == r1);
		assertTrue("Der Sued Raum wurde nicht richtig zugeteilt", r7.getSuedRaum() == r3);
		assertTrue("Der Ost Raum wurde nicht richtig zugeteilt", r7.getOstRaum() == r2);
		assertTrue("Der West Raum wurde nicht richtig zugeteilt", r7.getWestRaum() == r4);
		Raum r8 = new Raum();
		r8.setEnemy(true);
		assertTrue("Der Raum hat keinen Gegner", r8.isEnemy());
		Raum r9 = new Raum();
		r9.setBoss(true);
		assertTrue("Der Raum hat keinen Boss", r9.isBoss());
		Raum r10 = new Raum();
		r10.setDeadEnamy(true);
		assertTrue("Der Raum hat keinen toten Gegner", r10.isDeadEnamy());
		Raum r11 = new Raum();
		r11.setMegasuperboss(true);
		assertTrue("Der Raum hat keinen Mega Super Boss", r11.isMegasuperboss());*/
	}
}